# נדל"ן סקאוט — מפרט המערכת

מערכת פייתון שמזהה עסקאות נדל"ן אטרקטיביות בדרום הארץ, מדרגת אותן, שומרת
אקסל מסודר, ושולחת התראת מייל. ההרצה החוזרת היא **סקריפט פייתון ב-cron**
(לא אג'נט AI).

## עקרון על
אין קריאות LLM בזמן ריצה — הכול דטרמיניסטי, זול ואמין. הניקוד, הסינון
והפלט נגזרים כולם מ-`config.yaml`.

---

## הזרימה: listings-first (מודעות קודם)

**הרעיון:** יד2 הוא המקור הרחב והזול — סורקים אותו קודם ומנקדים הכול.
נדל"ן ממשלתי הוא הבדיקה היקרה והמדויקת — שולפים אותו **רק** למודעות
שכבר נראות מבטיחות, ורק לאזור הספציפי שלהן.

```
0. אינדקס יישובים (nadlan)  →  קוד למ"ס לכל עיר
1. סריקת יד2 דרך ScraperAPI →  כל הערים, כל טווח המחירים
2. SQLite: upsert + ירידות מחיר
3. סינון מקדים (חינם)       →  ניקוד מלא מול חציון ה-₪/מ"ר של המודעות עצמן
4. שער: score ≥ סף − מרווח  →  רק המועמדות ממשיכות:
       א. העשרה מדף המודעה (תיאור מלא + מצב נכס)
       ב. עסקאות שנסגרו בפועל **באזור הספציפי** (שכונה → יישוב)
       ג. ציון סופי
5. אקסל (4 גיליונות) + מייל אחד
```

### למה יש שער עם מרווח, ולא "ציון ≥ 70"
`price_gap` שווה 50 מתוך 100 נקודות. בלי בסיס השוואה כלשהו אי אפשר לעבור
70, ולכן "רק מי שעבר 70 ייבדק מול נדל"ן" היה חוסם את כל הצינור. הפתרון:

* **שלב א'** מודד את הפער מול **חציון המחיר המבוקש** של מודעות יד2 באותה
  עיר+חדרים. פרוקסי חינמי, בלי אף בקשה נוספת.
* **השער** נפתח ב-`alert_threshold − prescreen_margin` (ברירת מחדל 70−15=55),
  כי מודעה יכולה לקבל 62 מול מחירי מבוקש ו-80 מול עסקאות שנסגרו בפועל.
  המרווח קיים בדיוק כדי לא לפספס את המקרים שהשלב השני נועד לגלות.
* **שלב ב'** מחליף את הפרוקסי בפער מול עסקאות אמיתיות ומייצר את הציון הסופי.

רק מודעה שעברה את שלב ב' יכולה להיחשב "הזדמנות" (`stage == "final"`).

---

## מקורות נתונים

### 1. יד2 — דרך ScraperAPI (`scout/yad2.py`, `scout/scraperapi.py`)
yad2.co.il מוגן ב-Radware Bot Manager; בקשה ישירה מהשרת מקבלת דף Captcha.
**כל בקשה ליד2 עוברת דרך ScraperAPI** עם `country_code=il`.

מה נבדק בפועל (אוגוסט 2026):
* `country_code=il` בלבד, **בלי** `render` — עובד, מחזיר HTML מלא, **קרדיט אחד**.
  זו הדרך שבה המערכת עובדת.
* `render=true` — לא אמין (עבד פעם, אחר כך נכשל) ועולה פי 10.
* `premium=true` / `ultra_premium=true` — לא זמינים במסלול הנוכחי (403).
* ה-API הפנימי (`gw.yad2.co.il`) חסום **גם** דרך ScraperAPI.

לכן מושכים את **דף החיפוש ה-HTML** ומפרסרים את `<script id="__NEXT_DATA__">`:
`props.pageProps.feed.private` + `.agency` (20+20 מודעות לעמוד) ו-`pagination`.
`feed.yad1` (פרויקטים מקבלן) מסונן החוצה.

מפתח: `SCRAPERAPI_KEY` בסביבה. בקשה שנכשלת אינה מחויבת → retry זול.

**קודי ערים:** קודי הערים של יד2 הם קודי היישוב של הלמ"ס — אותם קודים
שמחזיר אינדקס היישובים של nadlan. אין טבלת קודים קשיחה בקוד.

### 2. נדל"ן ממשלתי — עסקאות שנסגרו בפועל (`scout/nadlan.py`)
`data.nadlan.gov.il` — ציבורי, ישיר, **בלי ScraperAPI ובלי עלות**:
* `api/index/setl_types.json` — אינדקס יישובים (קוד למ"ס + אוכלוסייה)
* `api/pages/settlement/buy/{code}.json` — נתוני יישוב + רשימת שכונות
* `api/pages/neighborhood/buy/{id}.json` — **נתוני שכונה** = האזור הספציפי

שניהם מחזירים מחיר עסקה ממוצע ב-12 החודשים האחרונים לפי מספר חדרים.

**התאמת אזור:** שם השכונה מיד2 ("שכונה ג'") מנורמל ומותאם לשם אצל נדל"ן
ממשלתי ("ג"). אם נמצאה שכונה עם נתונים — משתמשים בה; אחרת נופלים לרמת היישוב.
קטגוריות חדרים שאין עליהן עסקאות בשכונה משלימות מנתוני היישוב.

**מגבלות ידועות:**
* `api.nadlan.gov.il` (עסקה בודדת, עם מ"ר מדויק) מחזיר 403 — גם ישירות וגם
  דרך ScraperAPI עם יציאה ישראלית. לכן ₪/מ"ר הרשמי = מחיר ממוצע ÷ גודל
  אופייני (הגודל החציוני של מודעות אמיתיות באותה עיר+חדרים, ואם אין —
  `typical_size_sqm` מה-config).
* מספר העסקאות בפועל אינו נגיש ציבורית → `area_liquidity` מבוסס אוכלוסייה
  וצפיפות נתונים רשמיים.
* נתוני העסקאות מתעדכנים **בפיגור** (גרסה נוכחית 28-07-2025). הדו"ח והמייל
  מציינים את גרסת הנתונים בכל ריצה.

---

## התמדה (`scout/db.py`)
SQLite ב-`data/nadlan.db`:
* `listings` — `id, source, city, url, rooms, size_sqm, price, price_per_sqm,
  first_seen, last_seen, active, raw_json` + `neighborhood, street,
  property_type, condition_text, lat, lon`.
* `price_history` — `listing_id, price, seen_at`. נרשמת שורה **רק כשהמחיר
  משתנה**, כך שההיסטוריה רזה ומדויקת.
* `city_stats` — צילום מצב של הנתונים הרשמיים לכל ריצה.

**סימון מודעות לא-פעילות לפי ותק, לא לפי היעדרות מריצה:** הסריקה מוגבלת
ל-`yad2_max_pages_per_city`, ולכן מודעה קיימת עשויה פשוט לא להופיע בעמודים
שנמשכו. מודעה מסומנת `active=0` רק אם לא נראתה `inactive_after_days` ימים.

---

## מודל הניקוד — 0..100 (`scout/scoring.py`)
* **price_gap (0..40)** — כמה המחיר המבוקש מתחת לבסיס ההשוואה. 0% → 0 ;
  15% → 24 ; ≥25% → 40 (לינארי, capped ב-`price_gap_full_at_pct`).
  הבסיס: חציון מבוקש בשלב א', **חציון הקומפים** באזור בשלב ב'.
  בלי קומפים מספיקים — 0 נקודות, ולא "פער לא ידוע".
* **price_drop (0..15)** — ירידה ≥10% ובתוך 30 יום → מלא; דועך עד 90 יום.
  ירידה מעל `max_plausible_drop_pct` = שגיאת נתונים → 0 נקודות.
* **days_on_market (0..8)** — לפי `first_seen`; מעל 90 יום → מלא.
* **opportunity_keywords (0..7)** — מילים מ-`config.keywords` מול התגיות של
  יד2 ("בהזדמנות", "גמיש במחיר"), מצב הנכס, והתיאור המלא (זמין רק אחרי
  העשרה). מילים חופפות ("הזדמנות" בתוך "בהזדמנות") נספרות פעם אחת.
* **area_liquidity (0..5)** — אוכלוסייה + צפיפות נתונים רשמיים.
* **rental_yield (0..15)** — תשואת שכירות ברוטו מוערכת. 2% → 0 ; ≥5% → מלא.
* **area_appreciation (0..10)** — CAGR רב-שנתי באזור. 0% → 0 ; ≥6% → מלא.

**השער המקדים מנורמל:** תשואה ועליית ערך (25 נק') אינן ברות-השגה בשלב א',
ולכן `prescreen_gate()` מכפיל את `alert_threshold − prescreen_margin` ביחס
הניקוד הזמין (75/100). בלי הנרמול שער 55 היה דורש 73% מהאפשרי וחוסם את
כל הצינור.

### דירוג — מחליף את סף ההתראה הבודד
`tiers` ב-config: **לבדוק דחוף** (78) / **שווה בדיקה** (66) / **למעקב** (55).
* "לבדוק דחוף" דורש **גם** פער אמיתי (8–30%) **וגם אות תומך** — ירידת מחיר,
  ותק ארוך באוויר, או אזור נזיל. פער לבדו אינו מכתיר מודעה.
* פער > 30% = **חשוד**, לעולם לא בכותרת (לכל היותר "שווה בדיקה").
* בלי קומפים מספיקים — לכל היותר "למעקב".
* רק "לבדוק דחוף" נכנס לכותרת המייל.

### סוג ההזדמנות
`תשואה` / `עליית ערך` / `משולב` / `מציאת מחיר`, לפי `yield_good_pct`
ו-`cagr_good_pct`.

---

## השוואה like-for-like (`scout/comps.py`)
לכל מודעה: אותה שכונה (ובנפילה חזרה — אותו יישוב), חדרים ±0.5, חלון
12–18 חודשים, **חציון** ₪/מ"ר. פחות מ-`comps_min_count` תצפיות →
"אין מספיק עסקאות" וניקוד בלי פער.

**מגבלת מקור — חשוב:** `api.nadlan.gov.il` (עסקה בודדת, עם מ"ר) מחזיר 403,
ולכן `comp_count` סופר **תצפיות רבעון** של מחיר עסקה ממוצע לפי קטגוריית
חדרים — **לא עסקאות בודדות**. מאותה סיבה אי אפשר לסנן קומפים לפי גודל
±20%; הכלל מיושם כבדיקת שפיות על גודל המודעה מול הגודל הטיפוסי. ההסבר
המלא בראש `scout/comps.py`, והסייגים מופיעים ב-`out/latest.json`.

## דדופ התראות (`scout/alerts.py`)
מודעה נשלחת רק אם חצתה את הסף לראשונה, או שמאז ההתראה הקודמת הציון עלה
ב-`alert_score_rise`+ נקודות או שהמחיר ירד. השאר נספרות ומוצגות במייל
כ"דווחו כבר: N". המצב ב-`listings.first_alerted_at / last_alerted_score /
last_alerted_price`, ונרשם **רק אחרי שליחה מוצלחת**.

---

## תקציב ScraperAPI
בקשה מוצלחת = קרדיט אחד. **בקשה שנכשלת אינה מחויבת** — ולכן retry זול.

```
עלות ריצה ≈ (ערים × yad2_max_pages_per_city) + מספר המועמדות שהועשרו
ברירת מחדל:  17 × 3 = 51  +  עד 40  =  ~91 קרדיטים לריצה
```

חסמי בטיחות: `scraperapi_max_credits_per_run` (ברירת מחדל 200) עוצר את
הריצה בחן, ו-`max_final_checks_per_run` מגביל את שלב ההעשרה.
בקשות ל-nadlan אינן עולות דבר.

**מצב המסלול (נבדק 09-08-2026):** המסלול הוא **1,000 בקשות לחודש**, לא 5,000
— בדוק את המצב בפועל דרך `account_info()` ואל תסתמך על המספר כאן.
בנוסף, שיעור הכשל של yad2 דרך ScraperAPI גבוה מאוד (‎failedRequestCount‎
560 מתוך 632 בקשות היסטוריות ≈ 89%): אותו URL בדיוק מצליח בניסיון אחד
ונכשל ב-HTTP 500 "Protected" בניסיון הבא, וכל כשל לוקח ~56 שניות.
המשמעות המעשית: **הצינור תלוי ב-retry**, ריצה מלאה נמשכת ~30–40 דקות,
וייתכן שערים שלמות יחזרו ריקות בריצה בודדת. זו התנהגות המקור, לא באג —
`Yad2Status.blocked` והדו"ח מדווחים עליה בכל ריצה.

---

## פלט
`out/nadlan_YYYYMMDD.xlsx` + `out/latest.xlsx`, **חמישה** גיליונות:
1. **הזדמנויות** — "לבדוק דחוף" ו"שווה בדיקה", מודגשות בצבע לפי דירוג.
2. **כל המודעות** — כולל "שלב הבדיקה" (סינון מקדים / עסקאות אמיתיות).
3. **מעקב ירידות מחיר** — כולל סימון ירידה חריגה כשגיאת נתונים.
4. **השוואת אזורים** — חציון ₪/מ"ר רשמי, CAGR, נסרקו בריצה, מודעות פעילות.
5. **מגמת אזורים** — חציון ₪/מ"ר לכל שנה + מספר רבעונים עם עסקאות.

בכל שורת מודעה **שני קישורים לחיצים**: המודעה ביד2, וחיפוש בנדל"ן ממשלתי
הממוקד לעיר/אזור שלה.

עמודות השקיפות: חציון ₪/מ"ר בהשוואה, מס' קומפים, רמת ההשוואה, פער %,
ירידת מחיר %, ימים באוויר, תשואה ברוטו %, עליית ערך שנתית %, דירוג,
סוג ההזדמנות, הסבר בעברית פשוטה, ואיכות הנתונים.

### `out/latest.json`
כל מודעה עם כל השדות הנ"ל + פירוט הניקוד + ערכי הקומפים הגולמיים, ובלוק
`areas` עם חציונים שנתיים, מספר רבעונים עם עסקאות ו-CAGR לכל אזור.
כולל `data_caveats` שמתעד את מגבלות המקור במפורש.

### `out/charts/*.png`
* `area-<key>.png` — מגמת ₪/מ"ר לאורך 5 שנים (סמן חלול = כיסוי חלקי).
* `comps-<id>.png` — התפלגות הקומפים של עסקה מוכתרת, עם המודעה מסומנת.
עברית מיושרת דרך `python-bidi`; matplotlib אינו תומך ב-bidi מובנה.

## מייל
Gmail SMTP עם App Password. סודות מהסביבה: `GMAIL_ADDRESS`,
`GMAIL_APP_PASSWORD`, `ALERT_TO`, `SCRAPERAPI_KEY` — נטענים מ-
`~/.config/menutakim/secrets.env`. **מייל אחד לריצה**, גם כשאין הזדמנויות
(כדי שיהיה ברור שהמערכת חיה).

הכותרת מכילה **רק** מודעות "לבדוק דחוף" שעברו דדופ. "שווה בדיקה" מוצג
כרשימה מקוצרת, ו"דווחו כבר: N" מוצג כמונה.

## תזמון
`run.sh` מפעיל את ה-venv, טוען סודות ומפנה פלט ל-`logs/run-YYYYMMDD.log`.
cron יומי; המעבר לשבועי הוא שינוי שורה אחת (הערה בקובץ ה-crontab).

## עמידות
שום כשל של מקור לא מפיל את הריצה — כל שלב עטוף ב-try/except, נרשם ללוג,
והריצה ממשיכה עם מה שיש. בסוף כל ריצה נכתב דו"ח: כמה נסרק, כמה עבר סינון,
כמה נבדק מול עסקאות אמיתיות, כמה קרדיטים נוצלו, והאם יד2 נחסם.
